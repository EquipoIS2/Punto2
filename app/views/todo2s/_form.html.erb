<%= form_with(model: todo2, local: true) do |form| %>
  <% if todo2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo2.errors.count, "error") %> prohibited this todo2 from being saved:</h2>

      <ul>
      <% todo2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :todo2_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :todo2_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
