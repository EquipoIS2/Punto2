<%= form_with(model: todo3, local: true) do |form| %>
  <% if todo3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo3.errors.count, "error") %> prohibited this todo3 from being saved:</h2>

      <ul>
      <% todo3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :todo3_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :todo3_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
